schemaVersion: "2"
kind: mixin
name: gitlab-cli
version: "1"
displayName: GitLab CLI
description: >
  Provides the GitLab CLI (`glab` command) with envar-based token auth (GITLAB_TOKEN).
  Use the exit code of `glab api user >/dev/null` to check if the CLI is authenticated
  (`glab auth status` does not work with env tokens). Use `glab` for MRs, reviews,
  pipelines, and other GitLab operations.

commands:
  install:
    - command: |
        set -eu
        if ! command -v brew >/dev/null 2>&1; then
          NONINTERACTIVE=1 /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
        fi
      user: "1000"
      description: Install Homebrew (if absent)
    - command: |
        set -eu
        /home/linuxbrew/.linuxbrew/bin/brew install glab
      user: "1000"
      description: Install GitLab CLI
    - command: |
        set -eu
        mkdir -p /home/agent/.local/bin
        ln -sf /home/linuxbrew/.linuxbrew/bin/brew /home/agent/.local/bin/brew
        ln -sf /home/linuxbrew/.linuxbrew/bin/glab /home/agent/.local/bin/glab
      user: "1000"
      description: Symlink brew and glab to a directory on PATH

  startup:
    - command:
        - sh
        - -c
        - |
          set -eu

          # Respect manual override already present in glab config
          configured_host=$(/home/linuxbrew/.linuxbrew/bin/glab config get host --global 2>/dev/null || true)
          [ -n "$configured_host" ] && exit 0

          # Get workspace path written at init time
          workdir=$(cat /home/agent/.config/gitlab-cli/workdir 2>/dev/null || true)
          [ -z "$workdir" ] && exit 0

          # Require git
          command -v git >/dev/null 2>&1 || exit 0

          # Require a git repository at the workspace root
          git -C "$workdir" rev-parse --git-dir >/dev/null 2>&1 || exit 0

          # Require an origin remote
          origin_url=$(git -C "$workdir" remote get-url origin 2>/dev/null || true)
          [ -z "$origin_url" ] && exit 0

          # Extract the hostname from the URL
          # Handles: git@host:path  https://[user@]host[:port]/path  ssh://[user@]host[:port]/path
          host=""
          case "$origin_url" in
            git@*:*)
              host=$(printf '%s\n' "$origin_url" | sed 's/^git@//; s/:.*//')
              ;;
            *://*)
              host=$(printf '%s\n' "$origin_url" | sed 's|^[^/]*://||; s|^[^@]*@||; s|[/:?#].*||')
              ;;
          esac

          # Skip if it resolves to the default
          [ "$host" = "gitlab.com" ] && exit 0

          # Persist the detected host in glab's global config
          /home/linuxbrew/.linuxbrew/bin/glab config set host "https://$host" --global
          echo "gitlab-cli: host set to https://$host (detected from git origin)" >&2
      description: Auto-detect GitLab host from workspace git origin

  initFiles:
    - path: /home/agent/.config/gitlab-cli/workdir
      content: "${WORKDIR}"

memory: |
  GitLab CLI (`glab` command) is installed via Homebrew (`brew` command).
  Use `glab` for all GitLab operations, e.g. cloning, MRs, reviews, pipelines.
  
  The sandbox proxy handles GitLab authentication automatically by injecting credentials for CLI and HTTPS
  Git operations. You do not need to run `glab auth login` or configure Git credentials inside the
  sandbox — the proxy takes care of it transparently.
  Important: `glab auth status` will show "not logged in" inside the sandbox. This is expected and
  does not mean Git operations will fail. The proxy injects credentials at the network level,
  independently of the `glab` CLI auth state.

  `glab` also requires the host to be configured to determine which GitLab instance to target.
  The host is auto-detected from the workspace git origin URL (if it's a Git repo) and persisted in
  glab's global config. To override, run `glab config set host https://git.example.com --global`
  inside the sandbox (or via `sbx exec`) — the kit respects any host already present in glab config
  and skips auto-detection.
